let minimist,help,version,calculate;_c2c‍.x([["cli",()=>cli]]);_c2c‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_c2c‍.w("./help",[["help",["help"],function(v){help=v}]]);_c2c‍.w("./version",[["version",["version"],function(v){version=v}]]);_c2c‍.w("./calculate",[["calculate",["calculate"],function(v){calculate=v}]]);

// import { now } from './now';
// import { forecast } from './forecast';




       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;

    case 'add':
      calculate(args)
      break;

    case 'forecast':
    //   forecast(args);
      break;

    case 'config':
    //   configure(args);
      break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}